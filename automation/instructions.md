# Current instructions

Never write absolute filesystem paths to any files. Always use repository-relative paths or user-directory-relative paths such as `~/.codex`. This includes documentation, source, configuration, links, saved commands, logs, and copied prompt/tool output. Normalize paths before saving; resolve paths at runtime when a tool needs an absolute path.

Keep distilled instructions here and the timestamped raw prompt log in `automation/raw.md`, ignored by `.gitignore` by default. Preserve raw wording and line breaks except for required path normalization; this exception supersedes older verbatim-only logging instructions.

Updated: 2026-09-08T10:01:36.002433-04:00 (recording time).

Formalize the weak Hellinger three-point inequality from the supplied 7 September 2026 blueprint, including its equality characterization and necessary prerequisites. Keep the main proof in the existing `WeakHellinger/Auto/TheePointInequality.lean` (the on-disk spelling), in namespace `Auto`. The latest user prompt additionally authorizes isolating computation-intensive certificate work in the new `WeakHellinger/Auto/FiniteCertificate.lean`, also in namespace `Auto`, after the current build succeeds. This supersedes the single-file restriction for that extraction. Do not interrupt the current build unless it fails. Arrange an acyclic import graph and verify the split with a full build. After the real finite certificate builds and is extracted, the user authorizes a temporary dummy FiniteCertificate with at most ONE extra axiom solely for developing the remaining proof quickly. This is an explicit temporary exception to the no-new-axioms rule; it does not authorize sorries or other extra axioms. Restore the real certificate and remove the dummy axiom before final completion, then run the full build and audit the final inequality and equality characterization against only the standard allowed axioms. Continue until everything is completely done. Commit only when automation/Status.md is updated, and commit immediately after each such update. Include the corresponding proof work and records; do not push. This is clarified by the latest timestamped entry in `automation/raw.md`. The separately stated envelope theorem is not part of the requested inequality target.

Source: `blueprints/weak_hellinger_three_point_blueprint.tex`, Theorem `thm:main` and its dependency closure through `lem:boundary`.

Setup complete: Lean v4.34.0-rc2 and official Mathlib 572e4d091bcee94b35bdae44ff5a55687740f7c9 are pinned. Use `$env:ELAN_HOME = Join-Path $env:USERPROFILE '.elan'` before `lake build` or `lake env lean WeakHellinger/Auto/TheePointInequality.lean`. The default build covers the existing file directly.

Current proof position: `lem:finite-certificate` is active. All seven complete Bernstein tables, all seven homogeneous center evaluations, and every polynomial reconstruction component have passed staged Lean checks. The N2 certificate includes its checked balanced lookup tables, sparse seed, all 129 transform rows, all 26875 coefficient signs, exact counts, and witness. Audited dependencies are subsets of `propext`, `Classical.choice`, and `Quot.sound`. The exact chart-output wrappers and final certificate assembly have passed conditional composition diagnostics, but these do not establish their closed proofs. Whole-file validation is still running; do not mark this source item complete or advance to later source lemmas until the whole-file build and assembled `finite_certificate` axiom audit pass. Historical diagnostics and their resolutions are recorded in Status.md and ErrorReport.md.

The current arithmetic preserves the prescribed generators and coefficient predicates. It uses merge-based normalization and substitution, generic fixed-chunk reconstruction traces, sparse coefficient initialization, and checked binomial/array lookup equivalents. No placeholders or native-evaluation trust extensions are allowed. Keep all Lean work in the existing file and preserve the source statements. Do not launch import-based diagnostics until a current full build succeeds: the prior olean was removed during validation. After finite-certificate closes, continue in ledger order through chart strictness, squared-gap positivity, sign recovery, derivative positivity, and the complete interior/boundary inequality with exact equality cases. Commit immediately after every Status.md edit.

Current performance resolution: controlled diagnostics isolated excessive elaboration growth in the untyped inline multiplication-row functions, even when the test conclusion was trivial. Disabling asynchronous elaboration alone did not fix it. Explicit type annotations passed, while a named, explicitly typed row operation checked substantially faster. The actual closed node-125 multiplication certificate then passed all eleven row computations and its assembly with only propext and Quot.sound. The source now defines `polyMulRow` and a definitional `polyMulRows_eq_foldr` bridge, and uses them in all 158 multiplication steps and all nine assemblies (307 inline occurrences replaced). The polynomial data and equations are unchanged. The first file-level check caught PowerShell replacing Unicode in the new type annotation with question marks; this is corrected to the equivalent ASCII `Prod Exponent Int`. Set PowerShell OutputEncoding to UTF-8 before piping any Unicode Python or Lean source. All nine multiplication assemblies subsequently passed in a direct file check, through all three ratGap root audits; the three baseF conversion-success audits also passed. That run was stopped near 45 GB before whole-file completion. Do not mark finite-certificate complete yet.

Rational-node reconstruction: profiling found large closed computations being repeated during `change` and field matching in nine rational square/product nodes. The generic `rationalMul_eq_of_fields` and `rationalPow_two_eq_of_fields` lemmas passed with only propext and Quot.sound, and their applications to all nine literal nodes passed conditional composition checks with the same allowed axioms. The source now uses those helpers for nodes 125/126/127, 130/131/132, and 135/136/137. They retain the fixed expression tree's multiplication-by-one check and all denominator/exponent field checks. These replacements still need the current full-file validation; conditional product hypotheses do not prove the products.

Current validation: the previous ordinary full build failed only in the P1/P2 final wrappers after all closed arithmetic components passed. The exact P1/P2 conditional checks now pass using a generic polyHalfSubstitute_zero equality and explicit congruence/transitivity to align the zero-shift intermediate results; both audits use only propext and Quot.sound. This avoids the deep closed-definition matching that hit the recursion limit. The source now includes that symbolic equality and uses it in both wrappers; local maxRecDepth/maxHeartbeats settings match P3/B1. The slower limit-only diagnostic was stopped after the bridge alternative passed. A new ordinary full lake build of the corrected real source is now running. No snapshot flags, dummy axiom, placeholder, or file extraction has been introduced. Do not interrupt the full build unless it fails. Wait for successful full build and clean finite_certificate audit before extraction and temporary dummy development. Preserve LF source line endings by writing bytes: Python write_text on Windows translates LF to CRLF and creates a whole-file diff.

Boundary assembly diagnostic resolution: both stdin-only conditional tests passed the upper-j assembly with the same 32 explicitly typed row hypotheses. Applying an extra generic foldr-to-polyHalfRows bridge took 428 ms of final type checking; the existing direct foldr_trace32 assembly took 436 ms. Both audits use only propext and Quot.sound. Thus the test does not justify adding the bridge; neither diagnostic changes the closed row computations or proves their completion. No source changes were adopted. The normal build remains active. It survived a temporary peak around 48 GB and returned near 23 GB. Inspect actual available physical/virtual memory before interpreting a transient peak as a failure; do not stop solely because an earlier run was stopped at a similar number.


Latest continuation (2026-09-08T11:09:47.354333-04:00, recording time; see automation/raw.md): the user confirms the agreed extraction, verified split, temporary one-axiom dummy, completion of all remaining proof steps, and restoration/final audit. The corrected ordinary lake build has now PASSED (3472 jobs). Auto.finite_certificate depends only on propext, Classical.choice, Quot.sound; both P1/P2 wrappers passed. This supersedes all earlier pending-build notes above. Next: isolate the verified computation in FiniteCertificate.lean and verify the acyclic split before introducing the dummy.
